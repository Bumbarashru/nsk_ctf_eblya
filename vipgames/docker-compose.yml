services:
  vipgames:
    build: .
    image: 10.80.0.10:6789/vipgames:latest  # Добавлен тег для локального реестра
    container_name: vipgames
    ports:
      - "8642:3000"
    environment:
      - PORT=3000
      - HOST=0.0.0.0
      - DB_PATH=/app/data/vipgames.db
      - COOKIE_SECRET=vip-cookie-secret-change-me
    volumes:
      - vipgames-data:/app/data
    restart: unless-stopped

volumes:
  vipgames-data: